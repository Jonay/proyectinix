<% @columns_distribution="" %>

<div id="photoset">
  <div id="info">
    <strong>Fotos etiquetadas con <%= @tag %></strong>
  </div>
  <div>
  <% unless @photos.blank? %>
    <% for photo in @photos -%> <%= link_to image_tag(photo.image.url(:tiny)), picto_photo_url(photo), {:title => photo.title} -%> <% end -%>
  <% else %>
    No hay fotos con esta etiqueta todavía.
  <% end %>
  </div>
</div>