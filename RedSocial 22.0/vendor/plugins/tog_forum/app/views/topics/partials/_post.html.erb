<tr class="pair">
	<td colspan="2" class="col_50_10">
		<div class="left_col">
			<%= I18n.t('tog_forum.views.topics.posted',
								 :post_id => (post.id),  
								 :user => link_to(post.user.login, profile_path(post.poster_profile)),
								 :dtime => I18n.l(post.created_at, :format => :long))
 
            %>
		</div>

		<!-- Quitamos la opción de editar un post.
		<div class="right_col">
			<% if current_user && current_user == post.user %>
				<%= link_to I18n.t('tog_forum.views.posts.edit_post'), edit_forum_topic_post_path(post.topic.forum, post.topic, post), :class => 'button' %>
			<% end %>
		</div>
		-->
	</td>
</tr>

<tr>
	<td class="col_50_10">
		<div class="col_50_10" align="justify">
			<%= post.body %>
			<p></p>
		  <!--
		  <div class="right_col">
			<% unless @topic.locked? %>
				<h3><%= I18n.t('tog_forum.views.posts.add_reply') %></h3>

				<% if logged_in? %>
					<% form_for :post, :url => {:controller=>"posts", :action=>"create", :forum_id => @forum, :topic_id => @topic } do |f| %>
						<p>
							<%= f.text_area :body, :class => 'fieldbox', :size => '50x2' %>
						</p>            
						<div class="actions">
							<%= submit_tag I18n.t('tog_forum.views.posts.post_message'), {:class=>"button mainaction"} %>
						</div>
					<% end %>
				<% else %>
					<%= I18n.t('tog_forum.views.posts.only_registered_users') %>
				<% end %>
			<% end %>
	      </div>
		  -->
		</div>
		<!--<div class="right_col">
			<%= render :partial => 'shared/voting', :locals => { :voteable => post } %>
		</div>-->
	</td>
</tr>
